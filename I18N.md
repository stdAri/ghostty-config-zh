# 汉化与自动同步说明 (I18N)

本仓库是 [zerebos/ghostty-config](https://github.com/zerebos/ghostty-config) 的简体中文汉化 fork。

## 核心设计：零上游文件改动

上游源码（`src/` 等）在本仓库中**永远保持原样提交**。翻译只存在于词典文件
`i18n/zh-CN.json`（英文原文 → 中文译文）中，构建前由脚本临时改写工作区源码，
构建产物才是中文的。这样每周合并上游时几乎不会产生冲突。

## 文件

| 文件 | 作用 |
|------|------|
| `i18n/zh-CN.json` | 翻译词典。值为 `""` 表示待翻译 |
| `scripts/i18n/zh.mjs` | 全部工具：`extract` / `translate` / `apply` / `status` |
| `.github/workflows/sync.yml` | 每周一/四：合并上游 → 抽取新字符串 → 机器翻译 → 推送 |
| `.github/workflows/deploy.yml` | 每次 push：应用翻译 → 构建 → 部署到 GitHub Pages |

## 命令

```bash
node scripts/i18n/zh.mjs extract    # 扫描源码，把新字符串加进词典（译文为空）
node scripts/i18n/zh.mjs translate  # 用 Google Translate 免费端点翻译空条目
node scripts/i18n/zh.mjs apply      # 把译文写进工作区源码（仅本地/CI 构建前用）
node scripts/i18n/zh.mjs status     # 查看翻译覆盖率
```

`apply` 同时会把 `svelte.config.js` 的 `base: ""` 改为
`base: process.env.BASE_PATH ?? ""` 并设置 `relative: false`（绝对路径），
以适配 GitHub Pages 的子路径部署并保证深链/刷新可用。

**本地构建前运行 `apply` 后，记得在提交前还原源码：**

```bash
node scripts/i18n/zh.mjs apply && bun install && bun run build
git checkout -- src/ svelte.config.js   # 还原，翻译不要提交进源码
```

## 人工修正翻译

机器翻译（Google `gtx` 免费端点）难免生硬。直接编辑 `i18n/zh-CN.json`
里对应的值即可，下次部署自动生效；已翻译的条目不会被机器翻译覆盖。

## 翻译范围

目前覆盖 `src/lib/settings/registry.ts` 的 `name`/`description`/`note`
和 `src/lib/settings/navigation.ts` 的 `name`。想扩大范围，编辑
`scripts/i18n/zh.mjs` 顶部的 `TARGETS`。
